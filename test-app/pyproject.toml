[project]
name = "test-app"
version = "0.1.0"
description = "PyNest PR-1 smoke-test app — exercises engine contracts, http facade, all core features"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pynest-api",
    "httpx>=0.27.0",
    "pytest>=7.0",
    "pytest-asyncio>=0.23",
]

[tool.uv.sources]
pynest-api = { workspace = true }
